---
description: 指定範囲だけ実装。
agent: build
---

# /patch

承認された計画の範囲だけを実装してください。

## ルール
- 計画にない変更はしない
- 一度に変更するファイルは最小にする
- 各ファイル変更後に `git diff` で確認する
- テストがあれば必ず実行する

## 完了前チェック
- [ ] 計画との差分がないか
- [ ] `npm test` が通るか
- [ ] 不要な変更が含まれていないか
